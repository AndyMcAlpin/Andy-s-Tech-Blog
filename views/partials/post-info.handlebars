<article class="post">
    <div class="title">
        <a>{{title}}</a>
        <p>{{post_text}}</p>
    </div>
    <div class="meta">
        by {{user.username}} on {{created_at}}
        |
        <a href="/post/{{id}}">{{comments}} {{comments.length}}</a>
    </div>
</article>